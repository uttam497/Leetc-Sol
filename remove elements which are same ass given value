class Solution {
    public int removeElement(int[] nums, int val) {
        int i;
        int c=0;
        for(i=0;i<nums.length;i++)
        {
           if( nums[i]!=val)
           {
               nums[c++]=nums[i];
           }
            
           
        }
        return c;
    }
}
